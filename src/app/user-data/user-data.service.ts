// set(key, data) - сохраняет в localStorage данные data по ключу key
// get(key ) - возвращает данные из localStorage по ключу key
// clear(key?) - принимает на вход опциональный параметр key, если он есть,
// то удаляет из localStorage значение по key, если его нет - чистит localstorage полностью
